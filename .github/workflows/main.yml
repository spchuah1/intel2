#My First Workflow

name: workflow1
on: 
  workflow_dispatch:
  push:
  

jobs:
  job1:
    runs-on: self-hosted
    steps: 
      - name: step1
        run: |
          echo "hello from $name"
          env:
            name: Gagandeep
      - name: step2 for testing
        run: |
          cat /etc/os-release
          hostname
          touch /tmp/gagandeep1
          
## Name is not important in a step
      - run: uname -a
      
        
